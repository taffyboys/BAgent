[project]
name = "bagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.25.0",
    "openai>=2.45.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]
